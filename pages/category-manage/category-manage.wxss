/* category-manage.wxss - 自然有机/活泼风格 */

/* ========== CSS 变量 ========== */
page {
  --color-primary: #4A7C59;        /* 森林绿 */
  --color-primary-light: #6B9B7A;  /* 浅森林绿 */
  --color-primary-dark: #3D6349;   /* 深森林绿 */
  --color-secondary: #F5E6D3;      /* 米色/奶油色 */
  --color-accent: #E8A87C;         /* 温暖珊瑚橙 */
  --color-accent-light: #F4C4A8;    /* 浅珊瑚 */
  --color-bg: #FDF8F3;             /* 温暖的米白背景 */
  --color-bg-card: #FFFFFF;        /* 卡片白 */
  --color-text: #3D3D3D;           /* 主文字 - 深灰 */
  --color-text-light: #7A7A7A;     /* 辅助文字 */
  --color-text-muted: #A0A0A0;     /* 淡文字 */
  --color-success: #7CB342;        /* 成功绿 */
  --color-warning: #FFB74D;        /* 警告橙 */
  --color-danger: #E57373;         /* 柔和红 */
  --color-border: #E8E0D8;         /* 边框色 */

  --radius-sm: 16rpx;
  --radius-md: 24rpx;
  --radius-lg: 32rpx;
  --radius-xl: 48rpx;

  --shadow-sm: 0 4rpx 12rpx rgba(74, 124, 89, 0.08);
  --shadow-md: 0 8rpx 24rpx rgba(74, 124, 89, 0.12);
  --shadow-lg: 0 16rpx 48rpx rgba(74, 124, 89, 0.16);

  --font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Hiragino Sans GB', sans-serif;
}

/* ========== 页面容器 ========== */
.container {
  min-height: 100vh;
  background: var(--color-bg);
  padding: 24rpx;
  padding-bottom: 200rpx;
  box-sizing: border-box;
  position: relative;
  overflow: hidden;
}

/* 装饰性背景 */
.bg-decoration {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  z-index: 0;
  overflow: hidden;
}

.leaf {
  position: absolute;
  font-size: 48rpx;
  opacity: 0.15;
  animation: float 6s ease-in-out infinite;
}

.leaf-1 {
  top: 10%;
  right: 10%;
  animation-delay: 0s;
}

.leaf-2 {
  top: 30%;
  left: 5%;
  animation-delay: -2s;
}

.leaf-3 {
  bottom: 20%;
  right: 8%;
  animation-delay: -4s;
}

@keyframes float {
  0%, 100% {
    transform: translateY(0) rotate(0deg);
  }
  50% {
    transform: translateY(-20rpx) rotate(10deg);
  }
}

.bubble {
  position: absolute;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(74, 124, 89, 0.05), rgba(232, 168, 124, 0.05));
}

.bubble-1 {
  width: 200rpx;
  height: 200rpx;
  top: -50rpx;
  left: -50rpx;
}

.bubble-2 {
  width: 150rpx;
  height: 150rpx;
  bottom: 10%;
  right: -30rpx;
}

/* ========== 页面头部 ========== */
.page-header {
  position: relative;
  z-index: 1;
  padding: 32rpx 0 24rpx;
  text-align: center;
}

.header-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.header-title {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--color-primary);
  letter-spacing: 4rpx;
  position: relative;
}

.header-title::after {
  content: '';
  position: absolute;
  bottom: -8rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 80rpx;
  height: 6rpx;
  background: linear-gradient(90deg, var(--color-accent), var(--color-accent-light));
  border-radius: 3rpx;
}

.header-subtitle {
  font-size: 26rpx;
  color: var(--color-text-light);
  margin-top: 16rpx;
}

/* ========== 分类列表区域 ========== */
.category-list-container {
  position: relative;
  z-index: 1;
}

.section-title-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
  padding: 0 8rpx;
  flex-wrap: wrap;
  gap: 16rpx;
}

.section-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text);
}

.title-icon {
  font-size: 36rpx;
}

.category-count {
  font-size: 28rpx;
  color: var(--color-text-muted);
  font-weight: 400;
}

.sort-group {
  display: flex;
  gap: 12rpx;
}

/* 排序胶囊 */
.sort-pill {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 12rpx 20rpx;
  background: var(--color-secondary);
  border-radius: 40rpx;
  font-size: 26rpx;
  color: var(--color-text-light);
  transition: all 0.3s ease;
}

.sort-pill.active {
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
  color: #fff;
}

.sort-pill:active {
  transform: scale(0.95);
}

.sort-pill .sort-arrow {
  font-size: 20rpx;
  color: var(--color-accent);
  transition: transform 0.3s ease;
}

.sort-pill.active .sort-arrow {
  color: #fff;
}

.sort-pill .sort-arrow.desc {
  transform: rotate(180deg);
}

/* ========== 分类网格 ========== */
.category-grid {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 分类卡片 */
.category-card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 24rpx;
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);
  border: 2rpx solid var(--color-border);
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  animation: slideIn 0.4s ease-out backwards;
  animation-delay: var(--delay, 0s);
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.category-card:active {
  transform: scale(0.98);
  box-shadow: var(--shadow-sm);
}

.card-content {
  display: flex;
  align-items: center;
  gap: 24rpx;
  flex: 1;
}

.category-icon-wrap {
  width: 96rpx;
  height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--color-secondary) 0%, #FDF5EB 100%);
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: inset 0 2rpx 8rpx rgba(74, 124, 89, 0.1);
}

.category-icon-img {
  width: 64rpx;
  height: 64rpx;
  object-fit: contain;
}

.category-emoji {
  font-size: 52rpx;
}

.category-info {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.category-name {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text);
}

.category-date {
  font-size: 24rpx;
  color: var(--color-text-muted);
}

/* 操作按钮 */
.card-actions {
  display: flex;
  gap: 12rpx;
}

.action-btn {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 20rpx;
  font-size: 28rpx;
  transition: all 0.2s ease;
}

.action-btn:active {
  transform: scale(0.9);
}

.action-btn.edit {
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
}

.action-btn.delete {
  background: linear-gradient(135deg, #FFEBEE 0%, #FFCDD2 100%);
}

/* 空状态 */
.empty-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 40rpx;
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  border: 2rpx dashed var(--color-border);
}

.empty-illustration {
  margin-bottom: 24rpx;
}

.empty-icon {
  font-size: 96rpx;
  opacity: 0.6;
}

.empty-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: var(--color-text-muted);
  text-align: center;
}

/* ========== 添加按钮区域 ========== */
.add-category-section {
  position: fixed;
  bottom: 48rpx;
  left: 0;
  right: 0;
  display: flex;
  justify-content: center;
  z-index: 100;
}

.add-btn-wrap {
  position: relative;
}

.add-btn {
  padding: 8rpx;
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
  border-radius: 60rpx;
  box-shadow: 0 12rpx 32rpx rgba(74, 124, 89, 0.35);
  transition: all 0.3s ease;
}

.add-btn:active {
  transform: scale(0.95);
  box-shadow: 0 8rpx 24rpx rgba(74, 124, 89, 0.25);
}

.add-btn-inner {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 24rpx 48rpx;
  background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
  border-radius: 56rpx;
}

.add-icon {
  width: 44rpx;
  height: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fff;
  border-radius: 50%;
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-primary);
}

.add-text {
  font-size: 32rpx;
  font-weight: 700;
  color: #fff;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
  letter-spacing: 2rpx;
}

/* ========== 弹窗样式 ========== */
.dialog-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(61, 61, 61, 0.5);
  backdrop-filter: blur(8rpx);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.dialog-mask.show {
  opacity: 1;
  visibility: visible;
}

.dialog-container {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.8) translateY(0);
  width: 92%;
  max-width: 680rpx;
  max-height: 85vh;
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  z-index: 1001;
  opacity: 0;
  visibility: hidden;
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: var(--shadow-lg);
}

.dialog-container.show {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, -50%) scale(1) translateY(0);
}

.dialog-content {
  display: flex;
  flex-direction: column;
  max-height: 85vh;
  overflow: hidden;
}

.dialog-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 36rpx 32rpx 28rpx;
  border-bottom: 2rpx solid var(--color-border);
  flex-shrink: 0;
}

.dialog-title-wrap {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.dialog-emoji {
  font-size: 44rpx;
}

.dialog-title {
  font-size: 38rpx;
  font-weight: 700;
  color: var(--color-text);
}

.dialog-close {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: var(--color-text-muted);
  border-radius: 50%;
  transition: all 0.2s ease;
}

.dialog-close:active {
  background: var(--color-secondary);
  color: var(--color-text);
}

.dialog-form {
  padding: 32rpx;
  overflow-x: hidden;
  box-sizing: border-box;
  overflow-y: auto;
  flex: 1;
}

/* 表单项 */
.form-item {
  margin-bottom: 36rpx;
}

.form-item:last-child {
  margin-bottom: 0;
}

.item-label {
  display: flex;
  align-items: center;
  gap: 10rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 16rpx;
}

.label-icon {
  font-size: 32rpx;
}

.required {
  color: var(--color-danger);
  font-size: 28rpx;
  font-weight: bold;
}

.input-wrap {
  position: relative;
}

.item-input {
  width: 100%;
  height: 96rpx;
  padding: 0 28rpx;
  background: var(--color-secondary);
  border: 2rpx solid transparent;
  border-radius: var(--radius-sm);
  font-size: 30rpx;
  color: var(--color-text);
  box-sizing: border-box;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
}

.item-input:focus {
  background: #fff;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 6rpx rgba(74, 124, 89, 0.1);
}

.item-input::placeholder {
  color: var(--color-text-muted);
}

/* 图标选择器 */
.icon-selector {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.icon-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12rpx;
  padding: 20rpx;
  background: var(--color-secondary);
  border-radius: var(--radius-sm);
}

.icon-option {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 16rpx 8rpx;
  border-radius: 16rpx;
  background: #fff;
  border: 2rpx solid transparent;
  transition: all 0.2s ease;
}

.icon-option:active {
  transform: scale(0.95);
}

.icon-option.selected {
  border-color: var(--color-primary);
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
  box-shadow: 0 4rpx 16rpx rgba(74, 124, 89, 0.2);
}

.icon-emoji {
  font-size: 44rpx;
  margin-bottom: 4rpx;
}

.icon-name {
  font-size: 20rpx;
  color: var(--color-text-light);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
}

/* 上传区域 */
.upload-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx;
  border: 3rpx dashed var(--color-border);
  border-radius: var(--radius-sm);
  background: var(--color-secondary);
  transition: all 0.3s ease;
}

.upload-box:active {
  background: #FDF5EB;
  border-color: var(--color-primary);
}

.upload-box.has-image {
  padding: 16rpx;
  border-style: solid;
  border-color: var(--color-primary);
}

.upload-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.upload-icon {
  font-size: 64rpx;
}

.upload-text {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text);
}

.upload-hint {
  font-size: 22rpx;
  color: var(--color-text-muted);
}

.upload-preview {
  position: relative;
  width: 120rpx;
  height: 120rpx;
}

.preview-img {
  width: 100%;
  height: 100%;
  border-radius: 12rpx;
  object-fit: cover;
}

.preview-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.upload-box.has-image:active .preview-overlay {
  opacity: 1;
}

.preview-text {
  font-size: 20rpx;
  color: #fff;
  font-weight: 500;
}

.remove-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 16rpx;
  margin-top: 12rpx;
  font-size: 26rpx;
  color: var(--color-danger);
  transition: all 0.2s ease;
}

.remove-btn:active {
  opacity: 0.7;
}

/* 弹窗底部按钮 */
.dialog-footer {
  display: flex;
  gap: 20rpx;
  padding: 24rpx 32rpx 32rpx;
  border-top: 2rpx solid var(--color-border);
  flex-shrink: 0;
}

.dialog-btn {
  flex: 1;
  height: 96rpx;
  border-radius: var(--radius-md);
  font-size: 30rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  transition: all 0.3s ease;
}

.dialog-btn::after {
  border: none;
}

.dialog-btn.cancel {
  background: var(--color-secondary);
  color: var(--color-text-light);
}

.dialog-btn.cancel:active {
  background: #E8E0D8;
  transform: scale(0.98);
}

.dialog-btn.confirm {
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
  color: #fff;
}

.dialog-btn.confirm:active {
  opacity: 0.9;
  transform: scale(0.98);
}