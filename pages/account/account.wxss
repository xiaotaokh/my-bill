/* pages/account/account.wxss - 中性优先配色 */

/* CSS 变量定义 - Design Tokens */
page {
  --primary-600: #333333;
  --primary-500: #555555;
  --primary-100: #E5E5E5;
  --bg-base: #F5F5F5;
  --bg-card: #FFFFFF;
  --bg-subtle: #E5E5E5;
  --border-default: #CCCCCC;
  --text-default: #1A1A1A;
  --text-muted: #555555;
  --text-hint: #888888;
  --success: #0F8F6F;
  --error: #CF1322;
  --warning: #C27A2C;
}

.container {
  min-height: 100vh;
  background: #F5F5F5;
  position: relative;
  padding-bottom: 60rpx;
}

/* 头部背景 - 移除渐变和装饰 */
.header-bg {
  display: none;
}

/* 装饰气泡 - 移除 */
.bg-decoration {
  display: none;
}

/* 页面标题 */
.page-header {
  position: relative;
  z-index: 1;
  padding: 40rpx 40rpx 30rpx;
  text-align: center;
}

.page-title {
  display: block;
  font-size: 48rpx;
  font-weight: 700;
  color: #1A1A1A;
  margin-bottom: 10rpx;
}

.page-subtitle {
  display: block;
  font-size: 26rpx;
  color: #555555;
}

/* 加载状态 */
.loading-wrapper {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100rpx;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #CCCCCC;
  border-top-color: #333333;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.loading-text {
  font-size: 26rpx;
  color: #555555;
  margin-top: 20rpx;
}

/* 账号卡片 */
.account-card {
  position: relative;
  z-index: 1;
  margin: 20rpx 30rpx;
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 50rpx 40rpx;
  box-shadow: 0 4rpx 12rpx rgba(30, 42, 54, 0.08);
  border: 1rpx solid #CCCCCC;
}

/* 头像区域 */
.avatar-section {
  display: flex;
  justify-content: center;
  margin-bottom: 40rpx;
}

.avatar-wrapper {
  position: relative;
  width: 180rpx;
  height: 180rpx;
}

.avatar-img {
  width: 180rpx;
  height: 180rpx;
  border-radius: 50%;
  border: 4rpx solid #CCCCCC;
}

.avatar-placeholder {
  position: absolute;
  top: 0;
  left: 0;
  width: 180rpx;
  height: 180rpx;
  border-radius: 50%;
  background: #E5E5E5;
  display: flex;
  align-items: center;
  justify-content: center;
}

.placeholder-icon {
  font-size: 80rpx;
}

/* 信息区域 */
.info-section {
  text-align: center;
  margin-bottom: 30rpx;
}

.info-label {
  display: block;
  font-size: 24rpx;
  color: #888888;
  margin-bottom: 10rpx;
}

.info-value {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  color: #1A1A1A;
}

/* 分割线 */
.divider {
  height: 1rpx;
  background: #CCCCCC;
  margin: 30rpx 0;
}

/* 编辑提示 */
.edit-tip {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20rpx;
  margin-bottom: 20rpx;
  background: #E5E5E5;
  border-radius: 12rpx;
}

.tip-icon {
  font-size: 28rpx;
  margin-right: 10rpx;
}

.tip-text {
  font-size: 26rpx;
  color: #333333;
}

/* 编辑按钮 */
.edit-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 88rpx;
  border-radius: 12rpx;
  font-size: 30rpx;
  font-weight: 500;
  border: none;
  background: #333333;
  color: #FFFFFF;
}

.btn-icon {
  font-size: 32rpx;
  margin-right: 10rpx;
}

.btn-text {
  font-weight: 500;
}

/* 编辑模式 */
.edit-mode {
  padding: 10rpx 0;
}

/* 编辑提示信息 */
.edit-notice {
  padding: 20rpx 24rpx;
  margin-bottom: 20rpx;
  background: #E5E5E5;
  border-radius: 12rpx;
  border-left: 4rpx solid #333333;
}

.notice-text {
  font-size: 24rpx;
  color: #555555;
  line-height: 1.6;
}

.edit-row {
  display: flex;
  align-items: center;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #CCCCCC;
}

.edit-label {
  width: 100rpx;
  font-size: 28rpx;
  color: #1A1A1A;
  font-weight: 500;
}

.avatar-select-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding: 0;
  margin: 0;
  background: transparent;
  border: none;
  line-height: normal;
}

.avatar-preview {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  border: 2rpx solid #CCCCCC;
}

.avatar-empty-text {
  font-size: 26rpx;
  color: #888888;
}

.nickname-select-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding: 0;
  margin: 0;
  background: transparent;
  border: none;
  line-height: normal;
}

.nickname-text {
  font-size: 28rpx;
}

.nickname-text.filled {
  color: #1A1A1A;
}

.nickname-text.empty {
  color: #888888;
}

.select-arrow {
  font-size: 28rpx;
  color: #888888;
  margin-left: 10rpx;
}

.nickname-hidden-input {
  position: absolute;
  width: 0;
  height: 0;
  opacity: 0;
}

/* 随机选择提示 */
.random-tip {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx;
  margin: 30rpx 0 20rpx;
  background: #FFFFFF;
  border-radius: 12rpx;
  border: 1rpx dashed #333333;
}

.random-icon {
  font-size: 28rpx;
  margin-right: 10rpx;
}

.random-text {
  font-size: 26rpx;
  color: #333333;
}

/* 操作按钮 */
.edit-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 30rpx;
}

.cancel-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 12rpx;
  font-size: 30rpx;
  background: #E5E5E5;
  color: #555555;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.cancel-btn::after {
  border: none;
}

.confirm-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 12rpx;
  font-size: 30rpx;
  background: #333333;
  color: #FFFFFF;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.confirm-btn::after {
  border: none;
}

.confirm-btn.disabled {
  background: #CCCCCC;
}

/* 空状态 */
.empty-state {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 100rpx;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 28rpx;
  color: #888888;
}

/* 底部留白 */
.bottom-spacer {
  height: 60rpx;
}

/* 随机信息弹窗 */
.random-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  visibility: hidden;
  transition: all 0.2s ease;
}

.random-modal.show {
  opacity: 1;
  visibility: visible;
}

.random-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(30, 42, 54, 0.5);
}

.random-panel {
  position: relative;
  width: 600rpx;
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 40rpx;
  box-shadow: 0 8rpx 24rpx rgba(30, 42, 54, 0.15);
}

.random-header {
  text-align: center;
  margin-bottom: 30rpx;
}

.random-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #1A1A1A;
  margin-bottom: 10rpx;
}

.random-desc {
  font-size: 26rpx;
  color: #555555;
}

.random-body {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 30rpx 0;
}

.random-avatar-container {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.random-avatar-wrapper {
  position: relative;
}

.random-avatar-img {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 2rpx solid #333333;
}

.random-refresh-btn {
  width: 50rpx;
  height: 50rpx;
  background: #333333;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.refresh-icon {
  width: 24rpx;
  height: 24rpx;
}

.random-nickname-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-top: 20rpx;
}

.random-nickname-text {
  font-size: 32rpx;
  font-weight: 500;
  color: #1A1A1A;
}

.random-nickname-refresh {
  width: 40rpx;
  height: 40rpx;
  background: #E5E5E5;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1rpx solid #CCCCCC;
}

.refresh-icon-small {
  width: 20rpx;
  height: 20rpx;
}

.random-footer {
  display: flex;
  gap: 20rpx;
  margin-top: 30rpx;
}

.random-cancel-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: #E5E5E5;
  color: #555555;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.random-cancel-btn::after {
  border: none;
}

.random-confirm-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: #333333;
  color: #FFFFFF;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.random-confirm-btn::after {
  border: none;
}