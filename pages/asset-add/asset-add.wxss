/* asset-add.wxss - 温暖极简浅色主题 */

/* 容器 - 中性背景 */
.container {
  min-height: 100vh;
  background: var(--bg-base);
  padding: 24rpx;
  box-sizing: border-box;
}

.form {
  padding: 0;
}

/* 表单组 - 白色卡片 */
.form-group {
  background: var(--bg-card);
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(var(--shadow-rgb), 0.06);
  border: 1rpx solid var(--border-default);
}

.form-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 36rpx;
  padding-bottom: 24rpx;
  border-bottom: 1rpx solid var(--border-default);
}

.form-title {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-default);
  letter-spacing: 1rpx;
}

/* 表单项 */
.form-item {
  margin-bottom: 36rpx;
}

.form-item:last-child {
  margin-bottom: 0;
}

.item-label {
  display: flex;
  align-items: center;
  gap: 10rpx;
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-muted);
  margin-bottom: 20rpx;
}

.required {
  color: var(--error);
  font-size: 32rpx;
  font-weight: bold;
}

/* 输入框 */
.item-input,
.item-textarea {
  width: 100%;
  padding: 28rpx 32rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
  font-size: 30rpx;
  color: var(--text-default);
  box-sizing: border-box;
  border: 1rpx solid var(--border-default);
  transition: border-color 0.2s ease;
}

.input-placeholder {
  font-size: 30rpx;
  color: var(--text-hint);
  padding-left: 4rpx;
}

.item-input:focus,
.item-textarea:focus {
  background: var(--bg-card);
  border-color: var(--border-focus);
}

.item-input {
  min-height: 96rpx;
}

.item-textarea {
  min-height: 220rpx;
  resize: none;
  text-indent: 8rpx;
}

.textarea-placeholder {
  font-size: 30rpx;
  color: var(--text-hint);
  padding-left: 4rpx;
}

.char-count {
  display: block;
  text-align: right;
  font-size: 26rpx;
  color: var(--text-hint);
  margin-top: 16rpx;
  font-weight: 500;
}

/* 选择器 */
.item-picker {
  width: 100%;
}

.picker-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
  border: 1rpx solid var(--border-default);
  transition: border-color 0.2s ease;
}

.picker-content:active {
  background: var(--bg-card);
  border-color: var(--border-focus);
}

.picker-value {
  font-size: 30rpx;
  color: var(--text-default);
  font-weight: 500;
}

.picker-icon {
  font-size: 44rpx;
  color: var(--text-muted);
  font-weight: bold;
}

/* 类别网格 */
.category-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20rpx;
  width: 100%;
}

.category-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 28rpx 16rpx;
  background: var(--bg-card);
  border-radius: 12rpx;
  border: 1rpx solid var(--border-default);
  transition: all 0.2s ease;
  position: relative;
}

.category-item:active {
  transform: scale(0.96);
}

/* 选中状态 */
.category-item.selected {
  border-color: var(--primary-600);
  background: var(--bg-subtle);
}

/* 单选圆点样式 */
.radio-dot {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  border: 2rpx solid var(--border-default);
  background: var(--bg-card);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
  transition: all 0.2s ease;
}

.category-item.selected .radio-dot {
  border-color: var(--primary-600);
}

.radio-dot.checked .radio-inner {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: var(--primary-600);
}

.category-name {
  font-size: 26rpx;
  color: var(--text-muted);
  text-align: center;
  font-weight: 500;
  transition: all 0.2s ease;
}

.category-icon {
  font-size: 32rpx;
  margin-bottom: 8rpx;
  width: 100%;
  text-align: center;
}

/* 自定义图片图标的容器 */
.category-icon-wrap {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 8rpx;
}

/* 自定义图片图标的样式 */
.category-icon-img {
  width: 100%;
  height: 100%;
  border-radius: 12rpx;
  object-fit: cover;
}

.category-item.selected .category-name {
  color: var(--primary-600);
  font-weight: 600;
}

.category-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 0;
  grid-column: 1 / -1;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 16rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 26rpx;
  color: var(--text-hint);
  font-weight: 400;
}

.category-error {
  margin-top: 20rpx;
  padding: 16rpx 20rpx;
  background: var(--error-bg);
  border-radius: 12rpx;
  border-left: 4rpx solid var(--error);
}

.category-error text {
  font-size: 24rpx;
  color: var(--error);
  font-weight: 500;
}

/* 开关列表 */
.switch-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.switch-list-margin {
  margin-bottom: 24rpx;
}

/* 提示信息 */
.notice-tip {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 20rpx 24rpx;
  background: var(--warning-bg);
  border-radius: 12rpx;
  margin-bottom: 24rpx;
  border: 1rpx solid var(--warning);
}

.notice-icon {
  font-size: 28rpx;
  flex-shrink: 0;
}

.notice-text {
  font-size: 24rpx;
  color: var(--warning);
  line-height: 1.4;
}

.switch-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
  border: 1rpx solid var(--border-default);
  transition: background 0.2s ease;
}

.switch-item:active {
  background: var(--bg-card);
  transform: scale(0.98);
}

.switch-label {
  font-size: 30rpx;
  color: var(--text-default);
  font-weight: 500;
}

/* 开关颜色 - 使用全局变量 */
.switch-primary {
  color: var(--primary-600);
}

.switch-warning {
  color: var(--warning);
}

.switch-error {
  color: var(--error);
}

/* 退役日期容器 */
.retired-date-wrapper {
  background: var(--success-bg);
  border-radius: 12rpx;
  padding: 24rpx;
  border: 1rpx solid var(--success);
}

.retired-date-item {
  width: 100%;
}

.retired-date-item .item-label {
  margin-bottom: 16rpx;
}

/* 图标选择样式 */
.icon-selector {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

/* 图标分组容器 */
.icon-groups-container {
  overflow-y: auto;
  max-height: 800rpx;
  padding: 16rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
}

/* 分组标题 */
.group-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--text-default);
  margin-top: 24rpx;
  margin-bottom: 12rpx;
  padding-top: 12rpx;
  border-top: 1rpx solid var(--border-default);
}

.group-title.first {
  margin-top: 0;
  padding-top: 0;
  border-top: none;
}

/* 分组图标列表 */
.group-icons {
  display: flex;
  flex-wrap: wrap;
}

/* 单个图标项 */
.group-icon-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 12rpx 6rpx;
  border-radius: 12rpx;
  background: var(--bg-card);
  border: 1rpx solid var(--border-default);
  transition: all 0.2s ease;
  width: 19%;
  margin-right: 1.25%;
  margin-bottom: 8rpx;
  box-sizing: border-box;
}

/* 每行第5个去掉右边距 */
.group-icon-item:nth-child(5n) {
  margin-right: 0;
}

.group-icon-item:active {
  transform: scale(0.95);
}

.group-icon-item.selected {
  border-color: var(--primary-600);
  background: var(--bg-subtle);
}

.icon-emoji {
  font-size: 36rpx;
  margin-bottom: 6rpx;
}

.icon-name {
  font-size: 18rpx;
  color: var(--text-muted);
  text-align: center;
  line-height: 1.3;
  word-break: break-all;
}

.group-icon-item.selected .icon-name {
  color: var(--primary-600);
  font-weight: 600;
}

/* 旧的内置图标样式（保留兼容） */
.builtin-icons-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  justify-content: center;
  padding: 20rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
  margin-bottom: 20rpx;
}

.builtin-icon-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 16rpx 12rpx;
  border-radius: 12rpx;
  background: var(--bg-card);
  border: 1rpx solid var(--border-default);
  transition: all 0.2s ease;
  min-width: 80rpx;
}

.builtin-icon-item:active {
  transform: scale(0.95);
  border-color: var(--border-focus);
  background: var(--bg-subtle);
}

.builtin-icon {
  font-size: 40rpx;
  margin-bottom: 6rpx;
}

.builtin-icon-name {
  font-size: 20rpx;
  color: var(--text-muted);
}

.builtin-icon-item.selected {
  border-color: var(--primary-600);
  background: var(--bg-subtle);
}

.builtin-icon-item.selected .builtin-icon-name {
  color: var(--primary-600);
  font-weight: 600;
}

.current-icon-display {
  width: 100%;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.selected-icon-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  border: 1rpx solid var(--border-default);
  border-radius: 12rpx;
  background: var(--bg-card);
  flex: 1;
}

.selected-icon-text {
  font-size: 60rpx;
  margin-bottom: 10rpx;
}

.selected-icon-name {
  font-size: 24rpx;
  color: var(--text-muted);
}

.uploaded-preview-area {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  margin-top: 20rpx;
  width: 100%;
  max-width: 100%;
}

.image-container {
  position: relative;
  display: inline-block;
}

.uploaded-preview-image {
  width: 200rpx;
  height: 200rpx;
  max-width: 100%;
  border-radius: 12rpx;
  object-fit: cover;
  margin-bottom: 15rpx;
  border: 1rpx solid var(--border-default);
}

.remove-upload {
  position: absolute;
  top: -18rpx;
  right: -18rpx;
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: var(--error);
  color: var(--text-inverse);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  box-shadow: 0 2rpx 8rpx rgba(var(--error-rgb), 0.3);
  z-index: 10;
  transition: all 0.2s ease;
}

.remove-upload:hover {
  transform: scale(1.1);
}

.upload-area-large {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx;
  border: 1rpx dashed var(--border-default);
  border-radius: 12rpx;
  background: var(--bg-subtle);
  transition: all 0.2s ease;
  cursor: pointer;
}

.upload-area-large:active {
  background: var(--bg-card);
  border-color: var(--border-focus);
  transform: scale(0.98);
}

.upload-icon-large {
  font-size: 80rpx;
  margin-bottom: 16rpx;
}

.upload-text-large {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-default);
  margin-bottom: 8rpx;
}

.upload-desc-large {
  font-size: 24rpx;
  color: var(--text-hint);
}

/* 提交栏 */
.submit-bar {
  position: fixed;
  bottom: 40rpx;
  left: 0;
  right: 0;
  display: flex;
  justify-content: center;
  z-index: 100;
}

.submit-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--primary-600);
  border-radius: 60rpx;
  border: none;
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-inverse);
  box-shadow: 0 4rpx 12rpx rgba(var(--primary-600-rgb), 0.25);
  padding: 24rpx 48rpx;
  margin: 0 auto;
}

.submit-text {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-inverse);
  letter-spacing: 2rpx;
}

.submit-btn::after {
  border: none;
}

/* 资产类型选择器 */
.asset-type-selector {
  display: flex;
  gap: 20rpx;
  width: 100%;
}

.type-option {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 32rpx 24rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
  border: 1rpx solid var(--border-default);
  transition: all 0.2s ease;
}

.type-option:active {
  transform: scale(0.98);
}

.type-option.selected {
  border-color: var(--primary-600);
  background: var(--bg-subtle);
}

.type-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
}

.type-icon-img {
  width: 48rpx;
  height: 48rpx;
  margin-bottom: 12rpx;
}

.type-label {
  font-size: 28rpx;
  color: var(--text-muted);
  font-weight: 500;
}

.type-option.selected .type-label {
  color: var(--primary-600);
  font-weight: 600;
}

/* ========== 自定义emoji相关样式 ========== */

/* 自定义图标区域：左右分布 */
.custom-icon-row {
  display: flex;
  gap: 20rpx;
}

/* 上传图片区域 */
.upload-box-asset {
  flex: 1;
  min-width: 0;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx 20rpx;
  border: 1rpx dashed var(--border-default);
  border-radius: 12rpx;
  background: var(--bg-subtle);
  transition: all 0.2s ease;
  box-sizing: border-box;
}

.upload-box-asset:active {
  background: var(--bg-card);
  border-color: var(--border-focus);
}

.upload-box-asset.has-image {
  padding: 24rpx;
  border-style: solid;
  border-color: var(--primary-600);
}

/* emoji输入区域 */
.emoji-input-box-asset {
  flex: 1;
  min-width: 0;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx 20rpx;
  border: 1rpx dashed var(--border-default);
  border-radius: 12rpx;
  background: var(--bg-subtle);
  transition: all 0.2s ease;
  box-sizing: border-box;
}

.emoji-input-box-asset:active {
  background: var(--bg-card);
  border-color: var(--border-focus);
}

.emoji-input-box-asset.has-emoji {
  padding: 24rpx;
  border-style: solid;
  border-color: var(--primary-600);
}

/* 右上角删除按钮 */
.delete-corner-btn {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 44rpx;
  height: 44rpx;
  background: var(--error);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: var(--text-inverse);
  z-index: 10;
}

.delete-corner-btn:active {
  opacity: 0.8;
  transform: scale(0.9);
}

/* 上传图片占位符 */
.upload-placeholder-asset {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.upload-icon-asset {
  font-size: 48rpx;
}

.upload-text-asset {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-default);
}

.upload-hint-asset {
  font-size: 22rpx;
  color: var(--text-hint);
}

/* 上传图片预览 */
.upload-preview-asset {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.preview-img-asset {
  width: 80rpx;
  height: 80rpx;
  border-radius: 12rpx;
  object-fit: cover;
}

/* emoji占位符 */
.emoji-placeholder-asset {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.emoji-icon-asset {
  font-size: 48rpx;
}

.emoji-text-asset {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-default);
}

.emoji-hint-asset {
  font-size: 22rpx;
  color: var(--text-hint);
}

/* emoji预览 */
.emoji-preview-asset {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
}

.preview-emoji-asset {
  font-size: 48rpx;
}

/* ========== Emoji输入弹窗样式 ========== */

.emoji-dialog-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(var(--neutral-900-rgb), 0.5);
  z-index: 1100;
  opacity: 0;
  visibility: hidden;
  transition: all 0.2s ease;
}

.emoji-dialog-mask.show {
  opacity: 1;
  visibility: visible;
}

.emoji-dialog-container {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.8);
  width: 92%;
  max-width: 600rpx;
  background: var(--bg-card);
  border-radius: 16rpx;
  z-index: 1101;
  opacity: 0;
  visibility: hidden;
  transition: all 0.2s ease;
  overflow: hidden;
  box-shadow: 0 8rpx 24rpx rgba(var(--shadow-rgb), 0.2);
}

.emoji-dialog-container.show {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, -50%) scale(1);
}

.emoji-dialog-content {
  display: flex;
  flex-direction: column;
}

.emoji-dialog-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx;
  border-bottom: 1rpx solid var(--border-default);
}

.emoji-dialog-title-wrap {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.emoji-dialog-emoji {
  font-size: 40rpx;
}

.emoji-dialog-title {
  font-size: 34rpx;
  font-weight: 700;
  color: var(--text-default);
}

.emoji-dialog-close {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: var(--text-hint);
  border-radius: 50%;
  transition: all 0.2s ease;
}

.emoji-dialog-close:active {
  background: var(--bg-subtle);
  color: var(--text-default);
}

.emoji-dialog-body {
  padding: 32rpx;
}

.emoji-tip {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  padding: 20rpx 24rpx;
  background: var(--warning-bg);
  border-radius: 12rpx;
  margin-bottom: 24rpx;
}

.emoji-tip-icon {
  font-size: 32rpx;
  flex-shrink: 0;
}

.emoji-tip-text {
  font-size: 26rpx;
  color: var(--warning);
  line-height: 1.5;
  flex: 1;
}

.emoji-input-wrap {
  margin-bottom: 24rpx;
}

.emoji-input {
  width: 100%;
  height: 96rpx;
  padding: 0 28rpx;
  background: var(--bg-subtle);
  border: 1rpx solid var(--border-default);
  border-radius: 12rpx;
  font-size: 30rpx;
  color: var(--text-default);
  box-sizing: border-box;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
}

.emoji-input:focus {
  background: var(--bg-card);
  border-color: var(--border-focus);
}

.emoji-input::placeholder {
  color: var(--text-hint);
}

.emoji-examples {
  display: flex;
  align-items: center;
  flex-wrap: nowrap;
  gap: 16rpx;
  padding: 20rpx;
  background: var(--bg-subtle);
  border-radius: 12rpx;
}

.emoji-examples-label {
  font-size: 26rpx;
  color: var(--text-hint);
  white-space: nowrap;
  flex-shrink: 0;
}

.emoji-example {
  font-size: 40rpx;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  transition: all 0.2s ease;
  flex-shrink: 0;
}

.emoji-example:active {
  background: var(--bg-card);
  transform: scale(1.1);
}

.emoji-dialog-footer {
  display: flex;
  gap: 20rpx;
  padding: 24rpx 32rpx 32rpx;
  border-top: 1rpx solid var(--border-default);
}

.emoji-dialog-btn {
  flex: 1;
  height: 88rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  transition: all 0.2s ease;
}

.emoji-dialog-btn::after {
  border: none;
}

.emoji-dialog-btn.cancel {
  background: var(--bg-subtle);
  color: var(--text-muted);
}

.emoji-dialog-btn.cancel:active {
  background: var(--bg-card);
  transform: scale(0.98);
}

.emoji-dialog-btn.confirm {
  background: var(--primary-600);
  color: var(--text-inverse);
}

.emoji-dialog-btn.confirm:active {
  opacity: 0.9;
  transform: scale(0.98);
}

.amount-warning {
  font-size: 24rpx;
  color: #f59e0b;
  margin-top: 8rpx;
  padding-left: 4rpx;
}